@change wc.lit

This is a change file for wc.lit.

We will use this to change the title from `Word Count Program` to WC.

@replace
@title Word Count Program
@with
@title WC
@end

Additionally we will also change the comment type from // %s to /* %s */.

You can compile this file with `lit wc_ch.lit`. Then you will get two files:
`wc_ch.html` and `wc.c`.

@replace
@comment_type /* %s */
@with
@comment_type // %s
@end

@change_end

