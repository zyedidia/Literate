#!/bin/bash
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
if [ -f $DIR/../gen/lua ]
then
    $DIR/../gen/lua $DIR/lit.lua $DIR/../gen $@
else
    echo "Lua has not been built. Please run ./install.sh PLATFORM"
fi
